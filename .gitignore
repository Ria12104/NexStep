# NexStep — .gitignore

# config.js contains your publishable anon key — safe to commit (RLS protects data)
# Uncomment below only if you switch to a secret service-role key:
# config.js

# macOS
.DS_Store

# Editor
.vscode/
.idea/
